name: CI/CD Gatewey Service

on:
  push:
    branches: [ "main" ]
    paths:
      - 'back_end/gatewey-service/**'
      - '.github/workflows/gatewey-service.yml'
  workflow_dispatch:

jobs:
  build-and-secure:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        working-directory: ./back_end/gatewey-service
        run: mvn clean package -DskipTests

      # C'est ici que la magie opère avec le nouveau Token
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: rahilisalma93
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./back_end/gatewey-service
          file: ./back_end/gatewey-service/Dockerfile
          push: true
          tags: rahilisalma93/gatewey-service:latest